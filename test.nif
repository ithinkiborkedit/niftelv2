func add(x: int, y: int) -> int {
    return x+y
}

func identity[T](x: T) -> T{
    return x
}

struct Person {
    name: string
    age: int

    func greet(hi: string) -> string {
        return hi
    }

    func identity[T](x: T) -> T {
    return x
  }
}